# go-cve-db

### TODO:

- [ ] Divided CVEs based on years, and insert/create corresponding collections
- [ ] Add flag to indicate clone or pull
  - [ ] If clone, parse all CVEs and insert to database
  - [ ] If pull, upsert data
- [ ] Add argparser for command line usage
- [ ] Add webserver function to act as an API
